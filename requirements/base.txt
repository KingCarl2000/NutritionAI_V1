# Chế độ cài đặt cho thư mục hiện tại (cần có setup.py hoặc pyproject.toml)
-e .

# Web Framework & Server (API/Template responses)
fastapi
uvicorn
python-multipart

# Quản lý môi trường & Chứng chỉ
python-dotenv
PyYAML 
certifi
ipykernel

# Kết nối Database (PostgreSQL + pgvector)
sqlalchemy
pgvector
psycopg
psycopg_pool

# visualization
seaborn


# datasets
datasets
