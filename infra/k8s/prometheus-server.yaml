apiVersion: v1
kind: Service
metadata:
  name: prometheus-service
  namespace: default # Đã đổi thành default để khớp với Deployment của bạn
spec:
  type: ClusterIP
  ports:
    - port: 9090
      targetPort: 9090
      name: web
  selector:
    app: prometheus